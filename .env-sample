# Network Configuration
SOLANA_NETWORK=mainnet-beta

# Wallet Configuration
# Replace with your wallet's private key array from Solflare
# Format: [123,456,789,...]
WALLET_PRIVATE_KEY=[your,wallet,private,key,array]

# Token Configuration
TOKEN_NAME="Your Token Name"        # e.g., "My Game Token"
TOKEN_SYMBOL="TKN"                  # e.g., "MGT" (usually 2-4 characters)
TOKEN_DESCRIPTION="Token description"
TOKEN_DECIMALS=9                    # Common values: 9 for most tokens, 2 for stablecoins
TOKEN_SUPPLY=1000000000            # Total supply in base units (will be divided by 10^DECIMALS)

# Metadata Configuration
# After uploading metadata.json to Arweave, put the URL here
TOKEN_URI="https://arweave.net/YOUR_METADATA_TX_ID"

# Helius RPC Configuration
# Get your API key from https://dev.helius.xyz/
HELIUS_API_KEY=your_helius_api_key
